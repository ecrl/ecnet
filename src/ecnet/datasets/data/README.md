# Bundled property files

This directory contains the installable `.smiles` / `.target` pairs used by
`ecnet.datasets.load_*`.

Dataset cards (scope, units, counts, license notes) live in the Sphinx docs:

- `docs/source/data.rst` (*Bundled property datasets*)
